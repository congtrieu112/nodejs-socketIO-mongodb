import app from "./App";

const port = process.env.PORT || 3000;

// app.listen(port, (err) => {
//   if (err) {
//     return console.log(err)
//   }

//     return  console.log(`Server started on port ${app.server.address().port} :)`);

// })
app.getApp();
